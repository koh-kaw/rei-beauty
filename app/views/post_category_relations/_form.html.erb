<%= form_with(model: post_category_relation, local: true) do |form| %>
  <% if post_category_relation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post_category_relation.errors.count, "error") %> prohibited this post_category_relation from being saved:</h2>

      <ul>
        <% post_category_relation.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.number_field :post_id %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.number_field :category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
